import { createContext } from 'react'

export const QuestionContext = createContext<any>({})
export const SismoGroupContext = createContext<any>({})
export const SurvveyCreationContext = createContext<any>({})
export const SismoButtonContext = createContext<any>({})
export const AnswerContext = createContext<any>({})
export const StatDisplayContext = createContext<any>({})
